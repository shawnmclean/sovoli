---
id: "shawn-react-fundamentals"
title: "React Fundamentals"
description: "Core concepts and patterns in React development"
type: "book"
slug: "react-fundamentals"
isOrigin: true
isPublic: true
isDraft: false
chapterNumber: 1
verifiedDate: "2024-01-20"
query: "react fundamentals tutorial"
queryType: "query"
createdAt: "2024-01-10T00:00:00Z"
updatedAt: "2024-01-20T00:00:00Z"
userId: "shawn"
coverPhoto:
  url: "/orgs/users/shawn/notes/photos/react-fundamentals-cover.webp"
  alt: "React Fundamentals Cover"
  caption: "Core concepts and patterns in React development"
  category: "default"
  publicId: "shawn-react-fundamentals-cover"
inlinePhotos: []
---

# React Fundamentals

A comprehensive guide to React development.

## Components

Components are the building blocks of React applications.

### Functional Components

```jsx
function Welcome({ name }) {
  return <h1>Hello, {name}!</h1>;
}
```

### Class Components

```jsx
class Welcome extends React.Component {
  render() {
    return <h1>Hello, {this.props.name}!</h1>;
  }
}
```

## State Management

State allows components to manage data that changes over time.

### useState Hook

```jsx
import { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>Click me</button>
    </div>
  );
}
```

## Props

Props are how you pass data from parent to child components.

```jsx
function UserProfile({ user, age }) {
  return (
    <div>
      <h2>{user.name}</h2>
      <p>Age: {age}</p>
    </div>
  );
}
```
